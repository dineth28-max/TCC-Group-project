DB_ROOT_PASSWORD=csmas_root_password
DB_NAME=csmas
JWT_SECRET=change-this-to-a-long-random-string-before-any-real-deployment
# Dummy placeholder credentials — Phase 6's Notification Engine is fully wired against these;
# sends will fail and land in the delivery log as "Failed" until real SMTP credentials replace
# them here (no code change needed when that happens).
SMTP_USER=dummy@example.invalid
SMTP_PASS=dummy-password
FRONTEND_ORIGIN=http://localhost
